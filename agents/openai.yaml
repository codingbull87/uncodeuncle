interface:
  display_name: "Report Illustrator"
  short_description: "Consulting-style Markdown to HTML/PDF pipeline with strict gates"
  default_prompt: "Use $report-illustrator to run the gated workflow, and use explicit subagents in serial mode for planner, validator, and fragment generation."

policy:
  allow_implicit_invocation: false
